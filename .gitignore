.env
.env.local
server/.env
# Plaid Items catalog (access tokens + item IDs) — never commit.
server/.plaid-items.json
# Plaid structured logs (request_id, item_id, etc.) — local-only.
server/logs/
# Spotify runtime state — regenerated/rotated at runtime, never commit.
server/data/spotify-last.json
server/data/spotify-tokens.json
server/data/spotify-needs-reauth.flag
dist
.DS_Store
node_modules
server/node_modules
keys/

# Highlights: keep heavy phone .MOV originals out of git (optimized .mp4 committed instead)
src/assets/highlights/*.MOV
